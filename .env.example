# 后端服务端口 (容器内默认 3001)
BACKEND_PORT=3001

# 前端访问域名 (默认 localhost)
FRONTEND_DOMAIN=localhost

# 前端服务对外暴露端口 (默认 80)
FRONTEND_PORT=80

# 前端 HTTPS 对外暴露端口 (默认 443)
FRONTEND_SSL_PORT=443

# 是否启用 HTTPS (true/false)
ENABLE_HTTPS=false
